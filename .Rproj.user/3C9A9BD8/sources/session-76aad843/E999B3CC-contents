## App.R

library(shiny)
library(here) # Ensures the correct working directory

# Use relative paths to source the script
source(here::here("ShinyApp", "02-shiny-dashboard.R"))

# Run the app
shinyApp(ui = ui, server = server)
